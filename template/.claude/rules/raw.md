---
paths:
  - raw/**
---

# Raw 规则

- raw/ 目录是**只读原始资料层**，存放论文 PDF、转录文本、笔记等原始素材
- 禁止在 raw/ 中创建综述、计划、分析等**知识产出物**——这些必须写入 wiki/
- 禁止修改或删除 raw/ 下已有文件，除非用户明确要求
- 需要对原始资料做提炼或标注时，使用 `/ingest` 流程写入 wiki/source-summaries/
